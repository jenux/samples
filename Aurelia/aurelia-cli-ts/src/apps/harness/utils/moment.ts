/*import moment from 'moment-timezone';

// Wiki said that London GMT+00:00
// https://en.wikipedia.org/wiki/List_of_UTC_time_offsets
moment.tz.setDefault('Europe/London');

moment.range = {
    lastHour: function() {
        return { from: moment().startOf('hour'), to: moment().endOf('hour') };
    },
    last4Hour: function() {
        return { from: moment().subtract(3, 'hours').startOf('hour'), to: moment().endOf('hour') };
    },
    last8Hour: function() {
        return { from: moment().subtract(7, 'hours').startOf('hour'), to: moment().endOf('hour') };
    },
    last24Hours: function() {
        return { from: moment().subtract(23, 'hours').startOf('hour'), to: moment().endOf('hour') };
    },
    last7Days: function() {
        return { from: moment().subtract(6, 'day').startOf('day'), to: moment().endOf('day') };
    },
    last30Days: function() {
        return { from: moment().subtract(29, 'day').startOf('day'), to: moment().endOf('day') };
    },
    last90Days: function() {
        return { from: moment().subtract(89, 'day').startOf('day'), to: moment().endOf('day') };
    }
};

export { moment };*/
